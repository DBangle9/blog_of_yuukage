# 验证记录

2026-09-12，Windows，Hugo 0.147.7，Chrome 无头浏览器。

- 正式生产构建成功（39 个页面，含 RSS 与索引）。
- 首页仅显示正式博客文章，五篇旧创作完整保留。
- 五个旧故事 URL 与原站重新构建结果一致，均返回 200。
- 首页、文章、项目、关于、创作归档、角色档案、画廊、标签、分类及 404 模板可访问。
- 搜索可以匹配正文关键词，空结果有反馈；HTML 输入不会作为页面代码执行。
- 深色模式切换成功，刷新后保留用户选择。
- 改版说明目录包含四个章节，代码复制结果与正文一致。
- RSS 包含真实生产域名下的文章地址。
- 390px 手机视口下，主要页面无横向溢出。
- 自动化过程中没有浏览器 JavaScript 异常；已目视检查桌面和手机首页。

工作区的 `qa/` 保存预览截图；`scripts/verify.mjs` 是当前工作区的浏览器检查脚本，其中依赖路径为本机路径，移到其他电脑需要修改。

尚未验证 Cloudflare 账户端的构建、域名绑定或 TLS；这些需要实际接入账户后进行。
